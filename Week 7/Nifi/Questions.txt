1. What would happen if the input directory receives 10,000 files per minute?

The flow could experience performance issues if processors cannot handle the incoming rate. This Will cause queues and slowing down processing.

2. How would you modify this flow to send to Kafka instead of PutFile?

Interaction and transaction events could be sent to separate Kafka topics for scalable downstream processing.

3. What monitoring would you add for production?

I would monitor queue sizes, processing latency, and error rates